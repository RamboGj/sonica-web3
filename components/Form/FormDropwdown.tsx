export default function FormDropdow() {
  // const options = [{ option: 'cool' }]

  return (
    <div>
      <select name="" id="">
        {/* {options &&
          options.map((option: string) => {
            return (
              <option key={option} value="">
                {option}
              </option>
            )
          })} */}
      </select>
    </div>
  )
}
